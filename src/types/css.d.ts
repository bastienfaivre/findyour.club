// Allow CSS files to be imported as side-effects (e.g. import './globals.css')
// Next.js handles CSS imports at the bundler level; TypeScript only needs to know they exist.
declare module '*.css' {}
